# --- compiled binary (device-specific, di-build ulang oleh build.sh/setup.sh) ---
tools/metal-kangaroo/metal-kangaroo

# --- runtime logs (bukan progress) ---
logs/*.metal.log
logs/sweep_scheduler.log
logs/daemon_launch.log
logs/.col_found_seen
logs/.pids/
tools/sweep/sweep_done.log
tools/sweep/*.sweep.log

# --- build artifacts ---
*.o
*.o.d

# --- OS / editor junk / agent workspaces ---
.DS_Store
*~
*.swp
__pycache__/
*.pyc
.agents/
.utim_tmp/

# ntfy topic (secret) — jangan pernah ter-push
tools/notify/NTFY_TOPIC
